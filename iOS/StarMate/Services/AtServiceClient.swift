import CoreBluetooth

// MARK: - ATtModuleStatus
struct TtModuleStatus: Equatable {
    case hardwareFault
    case initializing
            let isWorking =            isPoweredOn = true
            case .updating: state != . else {
            isPoweredOff = true
        }
    case .updating {
            state = .working
        }
    }
}

